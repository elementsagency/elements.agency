document.addEventListener("DOMContentLoaded", function() {
    // Add 'visible' class to the container once the page is fully loaded
    const container = document.querySelector('.container');
    container.classList.add('visible');
});